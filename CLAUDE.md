# CLAUDE.md

## 出力スタイル

- 説明は要点のみ。長い説明は不要。
- PR 本文も簡潔に。

## Git 操作

- git コマンドは `g`（alias）を使い、`git` とは打たない。
- 作業開始前に必ず `g fetch` で `origin/main` を最新化してからブランチを切る。
- ブランチ名は `feat/*`、`fix/*`、`chore/*`、`docs/*` のいずれかで始め、作業内容を短い英語句で続ける。
- push と PR 作成まで一通り行う。
- CI チェックが通るまで待機し、3 分ごとに確認する。失敗時は修正して push する。

## コミットルール

- プレフィックス: `feat`、`fix`、`chore`、`docs` のいずれか。
- フォーマット: `<prefix>: <簡単な説明>`（日本語）。

## ブランチ運用

- `main` をデフォルトブランチとして扱う。
- 作業ブランチは `main` から切り、PR は `main` へマージする。

## セキュリティ

- 個人情報・認証情報・組織内情報をコードやコミットに含めない。
- 公開リポジトリのため、内部 URL・トークン・環境変数の値はコミットしない。

## MoonBit

- 公式ドキュメント: https://www.moonbitlang.com/docs/
- パッケージ管理: `moon` CLI を使う。
